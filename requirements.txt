fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.30
pydantic==2.7.4
python-jose==3.3.0
passlib[bcrypt]==1.7.4
prometheus-fastapi-instrumentator==7.0.0
httpx==0.27.0

# DEFECTO SEMBRADO (dependencia vulnerable):
# PyYAML 5.3.1 contiene la vulnerabilidad CVE-2020-14343 (ejecución arbitraria
# de código vía yaml.load con FullLoader). OWASP Dependency-Check la reporta y
# el Dependency Gate se dispara. Tras la remediación, actualizar a PyYAML>=6.0.1.
PyYAML==5.3.1

# Dependencias de test
pytest==8.2.2
pytest-cov==5.0.0
